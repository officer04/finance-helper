/**
 * The interface represents a successful registration or authorization
 */
export interface UserResponse {
  /**
 * The interface represents a successful registration or authorization
 */
  bearerToken: string;
  /**
 * , if exists
 */
  userId?: string
}

